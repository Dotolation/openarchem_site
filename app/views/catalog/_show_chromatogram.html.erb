<% doc_presenter = show_presenter(document) %>
<%# partial to display samples documents in catalog show view -%>

<% data_hashes = chromatogram_show_data(params["id"])%>

<%= render :partial => "new_chromatogram_show" , :locals => {:data_hashes => data_hashes} %>
